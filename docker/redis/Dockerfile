# ============================================================
# Redis 自定义配置
# ============================================================
FROM redis:7-alpine

# ============================================================
# 复制配置文件
# ============================================================
COPY docker/redis/redis.conf /etc/redis/redis.conf

# ============================================================
# 设置命令
# ============================================================
CMD ["redis-server", "/etc/redis/redis.conf"]
